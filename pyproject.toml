[project]
name = "invoice-ai-reader"
version = "0.1.0"
description = "Automated invoice data extraction using Google Gemini API and Streamlit"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
